# Baseline Evidence — 2026-07-21

Evidence timestamp: 2026-07-21T18:53:13Z  
Repository: `/home/andrew/Documents/Projects/structured-agents-v2`  
Commit: `90725a56f28c6a5a09c0a93a31afcb15f3dfa504` on `main`

## Worktree

Initial `git status --short --branch`:

```text
## main...origin/main
?? .scratch/projects/12-structured-agents-v2-library-study/
?? .scratch/projects/13-xgrammar-and-batching-todo/
```

Projects 12 and 13 are pre-existing user work and were preserved. This project is the only new repository tree created by this session.

## Installed library environment

Command: `devenv shell -- python -c <dependency and signature inspection>`  
Exit: 0

```text
Python 3.13.13
structured-agents 0.3.0
dbos 2.23.0
pydantic-ai-slim 2.11.0
pydantic 2.13.3
httpx 0.28.1
xgrammar not installed
DBOSAgent.run(...) -> AgentRunResult[Any]
Queue.enqueue_async(...) -> WorkflowHandleAsync[R]
WorkflowHandleAsync.get_result(...) -> R
```

Local dependency source confirms that `DBOSAgent` registers the callable `dbos_wrapped_run_workflow`, while its public `run` method is a normal wrapper. The registered workflow returns `AgentRunResult[Any]`. DBOS queue submission rejects unregistered functions before recording input.

## Documented quality baseline

| Command | Exit | Concise result |
|---|---:|---|
| `devenv shell -- pytest` | 0 | 32 passed, 1 skipped in 13.89 s |
| `devenv shell -- ruff check src tests` | 0 | All checks passed |
| `devenv shell -- ty check src tests/typecheck_constraint.py` | 0 | All checks passed |
| `devenv shell -- ruff format --check src tests` | 1 | Eight files would be reformatted; 19 already formatted |
| `devenv shell -- uv build --out-dir /tmp/structured-agents-build-tLgbSP` | 0 | Wheel and sdist built successfully |

The format failures are unchanged from the earlier review: `agent.py`, `config.py`, `plane.py`, `live_crash_worker.py`, `test_agent.py`, `test_config.py`, `test_live.py`, and `test_plane.py`.

The wheel contains only the package and distribution metadata. The fresh sdist is contaminated by deployment and vendor content, including `deploy/sglang/native/pyproject.toml`, `deploy/vllm/native/vendor/vllm-gguf-plugin/pyproject.toml`, and 17 vendored-plugin test files. This reconfirms CR-08.

## Focused finding probe

Command: `devenv shell -- python .scratch/projects/14-specialist-runtime-architecture-research/evidence/probes/revalidate_findings.py`  
Exit: 0

```json
{
  "config_allowlist_observed": ["module_a", "module_a"],
  "empty_all_of": {"allowed": true, "reason": ""},
  "invalid_settings": {"temperature": "cold", "max_tokens": -1},
  "malformed_approval": {"allowed": true, "reason": "string"},
  "real_agent_queue": {
    "exception": "DBOSWorkflowFunctionNotFoundError",
    "message": "run is not a registered workflow function"
  },
  "schema_cast": {"runtime_type": "dict", "value": {"value": 7}}
}
```

The two-thread probe uses barriers before and after reading the active allowlist, proving that separate calls can both observe one request's module allowlist.

## Current inference-service snapshot

No service was modified.

| Service | Current state | Listener/GPU evidence |
|---|---|---|
| vLLM | active since 2026-07-18 23:50:27 EDT | `127.0.0.1:8000`; GPU 1 uses 10,803 MiB |
| SGLang | inactive / no installed unit | no `:8002` listener |
| llama.cpp | active since 2026-07-15 11:32:00 EDT | `127.0.0.1:8001`; GPU 0 uses 8,401 MiB |

`GET /health` on vLLM returned 200. `GET /v1/models` returned only `base`, rooted at `unsloth/gemma-4-12B-it-qat-GGUF:UD-Q4_K_XL`, with `max_model_len=16384`; no LoRA adapters are registered.

The locked deployment environment reports Python 3.12.13, vLLM 0.25.0, XGrammar 0.2.3, and `vllm-gguf-plugin 0.0.4+structuredagents.gemma4.3`.

No new constrained generation was sent in this baseline snapshot. Preserved project-10 artifacts remain the reproducible evidence that this exact vLLM/GGUF/XGrammar profile previously passed raw and public-library schema, regex, choice, and grammar tests after the deployment environment was reconciled. Those artifacts do not prove LoRA or mixed batching.

